<p id="notice"><%= notice %></p>

<h1>Add Category Tables</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_category_tables.each do |add_category_table| %>
      <tr>
        <td><%= link_to 'Show', add_category_table %></td>
        <td><%= link_to 'Edit', edit_add_category_table_path(add_category_table) %></td>
        <td><%= link_to 'Destroy', add_category_table, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Category Table', new_add_category_table_path %>
